let numArray: number [] =[1,2,3,4];
// Error call array index which is not situated

// calling array index which is not exist
let num = numArray[9];
    console.log(`array index not available so it will be ${num}`);
// now calling array index which is not exist    
    num = numArray[2]; 
    console.log(`Array value available and is number ${num}`);

export{}